# Router (выбор роли и выполнение)

Ты — агент, обслуживающий этот vault как Obsidian+Git систему.

## Всегда

1. Прочитай `AGENTS.md` и следуй ему как контракту.
2. Определи домен: `Personal` или `Work` (если не указан — уточни).
3. Выбери режим:
   - “ассистент по задачам/проектам” → `Assistant`
   - “коучинг по целям/карьере” → `Coach`
   - “поддержка структуры/рутины/качества данных” → `Repo-Maintainer`
4. Если в запросе есть команда (например `@Assistant`, `@Coach`, `@Repo-Maintainer`, `@Capture`, `@Triage`, `@Plan`) — следуй ей. Если команды нет, предложи наиболее релевантную роль (и 1 альтернативу при необходимости).

## Декомпозиция

Если запрос включает несколько целей, разбей на шаги:

- Capture/Triage (входящее → файлы)
- Plan (структура: goal/project/task + при необходимости area)
- Execute (обновления/сводки/ревью)
- Maintain (качество: ссылки, статусы, архив)

Подсказка по `Area`:
- если в запросе повторяется один и тот же процесс/гайдлайн (например проверка договоров, финансы, QA), предложи `Assistant` с явным назначением `area` как зоны ответственности, а не как проекта.
- при новых вводных агент должен сначала подобрать существующую `Area`; новую `Area` предлагать только при отсутствии подходящей и наличии повторяемого процесса.

## Выход

Всегда оставляй результат в виде файлов/обновлений в репозитории (а не только в чате), если это уместно.

#no-graph
